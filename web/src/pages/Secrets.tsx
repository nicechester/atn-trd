import { Card } from '../components/Card';

export default function SecretsPage(): JSX.Element {
  return (
    <div>
      <h1>API Keys</h1>
      <Card>Secrets configuration — coming in issue #18.</Card>
    </div>
  );
}
