import { Card } from '../components/Card';

export default function SettingsPage(): JSX.Element {
  return (
    <div>
      <h1>Settings</h1>
      <Card>Settings form — coming in issue #18.</Card>
    </div>
  );
}
